<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>反馈管理 - API 中转服务</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet">
    <link href="/bootstrap-icons/bootstrap-icons.css" rel="stylesheet">
    <link href="/css/modern-dark.css" rel="stylesheet">
    <style>
        .main-wrapper { display: flex; min-height: 100vh; }
        .sidebar { width: 260px; flex-shrink: 0; z-index: 100; }
        .content-area { flex-grow: 1; padding: 2rem; overflow-y: auto; }
        .message-preview { max-width: 300px; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
    </style>
</head>
<body>
    <div class="main-wrapper">
        <aside class="sidebar modern-sidebar p-3 d-flex flex-column">
            <div class="p-3 mb-4 d-flex align-items-center">
                <div class="bg-primary bg-opacity-10 p-2 rounded-3 me-3">
                    <i class="bi bi-cpu-fill text-primary"></i>
                </div>
                <h5 class="mb-0 fw-bold glow-text">管理后台</h5>
            </div>
            <nav class="nav flex-column flex-grow-1">
                <a class="nav-link" href="/admin/dashboard"><i class="bi bi-speedometer2 me-2"></i> 仪表板</a>
                <a class="nav-link active" href="/admin/feedback"><i class="bi bi-chat-square-dots me-2"></i> 反馈管理</a>
                <a class="nav-link" href="/admin/reports"><i class="bi bi-flag me-2"></i> 举报管理</a>
                <a class="nav-link" href="/admin/blacklist"><i class="bi bi-shield-slash me-2"></i> 黑名单管理</a>
                <a class="nav-link" href="/admin/whitelist"><i class="bi bi-shield-check me-2"></i> 白名单管理</a>
                <a class="nav-link" href="/admin/logs"><i class="bi bi-terminal me-2"></i> API 日志</a>
            </nav>
            <div class="mt-auto pt-3 border-top border-secondary border-opacity-25">
                <a class="nav-link text-danger" href="#" id="logoutBtn"><i class="bi bi-power me-2"></i> 退出登录</a>
            </div>
        </aside>

        <main class="content-area">
            <header class="d-flex justify-content-between align-items-center mb-5">
                <div>
                    <h2 class="fw-bold mb-1">反馈管理</h2>
                    <p class="text-secondary mb-0">处理用户提交的意见与建议</p>
                </div>
                <div class="d-flex gap-2">
                    <a href="/admin/feedback/export" class="btn btn-outline-light btn-modern">
                        <i class="bi bi-download me-1"></i> 导出 CSV
                    </a>
                    <button class="btn btn-primary-modern btn-modern" onclick="loadFeedback()">
                        <i class="bi bi-arrow-clockwise me-1"></i> 刷新列表
                    </button>
                </div>
            </header>

            <div class="glass-card overflow-hidden">
                <div class="table-responsive">
                    <table class="table modern-table mb-0">
                        <thead>
                            <tr>
                                <th>时间</th>
                                <th>用户</th>
                                <th>联系方式</th>
                                <th>内容</th>
                                <th>状态</th>
                                <th>操作</th>
                            </tr>
                        </thead>
                        <tbody id="feedbackTableBody">
                            <tr>
                                <td colspan="6" class="text-center py-5">
                                    <div class="spinner-border text-primary" role="status"></div>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </main>
    </div>

    <!-- 反馈详情模态框 -->
    <div class="modal fade" id="detailModal" tabindex="-1">
        <div class="modal-dialog modal-lg">
            <div class="modal-content glass-card p-2" style="background: var(--sidebar-bg);">
                <div class="modal-header border-0">
                    <h5 class="modal-title fw-bold">反馈详情</h5>
                    <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-4">
                        <label class="text-secondary small mb-1">反馈内容</label>
                        <div class="p-3 rounded-3 bg-dark bg-opacity-50 border border-secondary border-opacity-25" id="modalMessage"></div>
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="text-secondary small mb-1">用户姓名</label>
                            <p id="modalName" class="fw-bold">-</p>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="text-secondary small mb-1">联系邮箱</label>
                            <p id="modalEmail" class="fw-bold">-</p>
                        </div>
                    </div>
                </div>
                <div class="modal-footer border-0">
                    <button type="button" class="btn btn-outline-light btn-modern" data-bs-dismiss="modal">关闭</button>
                    <button type="button" class="btn btn-primary-modern btn-modern" id="markProcessedBtn">标记为已处理</button>
                </div>
            </div>
        </div>
    </div>

    <script src="/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script>
        let currentId = null;
        const detailModal = new bootstrap.Modal(document.getElementById('detailModal'));

        document.addEventListener('DOMContentLoaded', loadFeedback);

        async function loadFeedback() {
            try {
                const response = await fetch('/admin/feedback/data');
                const result = await response.json();
                const tbody = document.getElementById('feedbackTableBody');

                if (result.success && result.data.length > 0) {
                    tbody.innerHTML = result.data.map(item => `
                        <tr>
                            <td class="small text-secondary">${new Date(item.created_at).toLocaleString()}</td>
                            <td>${item.name || '<span class="text-muted">匿名</span>'}</td>
                            <td class="small">${item.email || '-'}</td>
                            <td><div class="message-preview small text-secondary">${item.message}</div></td>
                            <td>
                                <span class="badge badge-modern ${item.status === 'processed' ? 'bg-success-subtle' : 'bg-warning-subtle'}">
                                    ${item.status === 'processed' ? '已处理' : '待处理'}
                                </span>
                            </td>
                            <td>
                                <button class="btn btn-sm btn-outline-primary btn-modern" onclick="showDetail(${item.id})">
                                    <i class="bi bi-eye"></i>
                                </button>
                                <button class="btn btn-sm btn-outline-danger btn-modern ms-1" onclick="deleteFeedback(${item.id})">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </td>
                        </tr>
                    `).join('');
                } else {
                    tbody.innerHTML = '<tr><td colspan="6" class="text-center py-5 text-secondary">暂无反馈记录</td></tr>';
                }
            } catch (error) {
                console.error('加载失败:', error);
            }
        }

        async function showDetail(id) {
            try {
                const response = await fetch(`/admin/feedback/${id}`);
                const result = await response.json();
                if (result.success) {
                    const item = result.data;
                    currentId = id;
                    document.getElementById('modalMessage').textContent = item.message;
                    document.getElementById('modalName').textContent = item.name || '匿名';
                    document.getElementById('modalEmail').textContent = item.email || '-';

                    const processBtn = document.getElementById('markProcessedBtn');
                    if (item.status === 'processed') {
                        processBtn.style.display = 'none';
                    } else {
                        processBtn.style.display = 'block';
                        processBtn.onclick = () => markProcessed(id);
                    }

                    detailModal.show();
                }
            } catch (error) {
                console.error('获取详情失败:', error);
            }
        }

        async function markProcessed(id) {
            try {
                const response = await fetch(`/admin/feedback/${id}/processed`, { method: 'PUT' });
                const result = await response.json();
                if (result.success) {
                    detailModal.hide();
                    loadFeedback();
                }
            } catch (error) {
                console.error('更新失败:', error);
            }
        }

        async function deleteFeedback(id) {
            if (!confirm('确定要删除这条反馈吗？')) return;
            try {
                const response = await fetch(`/admin/feedback/${id}`, { method: 'DELETE' });
                const result = await response.json();
                if (result.success) {
                    loadFeedback();
                }
            } catch (error) {
                console.error('删除失败:', error);
            }
        }

        document.getElementById('logoutBtn').addEventListener('click', async (e) => {
            e.preventDefault();
            if (confirm('确定要退出登录吗？')) {
                await fetch('/admin/logout', { method: 'POST' });
                window.location.href = '/admin/login';
            }
        });
    </script>
</body>
</html>
